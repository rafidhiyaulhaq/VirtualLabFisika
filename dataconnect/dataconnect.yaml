specVersion: "v1beta"
serviceId: "VirtualLabFisika"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "VirtualLabFisika-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
